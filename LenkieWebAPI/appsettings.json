{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost;Database=Lankie;Trusted_Connection=True;TrustServerCertificate=True"
  },
  "ApiSettings": {
    "JwtOptions": {
      "Secret": "This is used to sign and verify jwt tokens, replace it here with own secret",
      "Issuer": "lenkie-auth-api",
      "Audience": "lenkie-client"
    },
    "Secret": "This is used to sign and verify jwt tokens, replace it here with own secret",
    "Issuer": "lenkie-auth-api",
    "Audience": "lenkie-client"

  }
}
